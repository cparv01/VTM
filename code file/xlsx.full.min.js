// File: /checkmarxmasterscan-CentralProjeForInfosec/Central/Layouts/Central/FPRAssets/scripts/xlsx.full.min.js

// Lines leading up to the vulnerability (for context):
// ...

// Vulnerable function at line 13369:
function createHash(input) {
    // Example of weak hashing algorithm (MD5)
    // This is a potential security vulnerability as MD5 is considered weak and prone to collisions
    var hash = crypto.createHash('md5');  //  ->  10
    hash.update(input);
    return hash.digest('hex');
}

// More code...
// ...

// Note: the real file might have compressed or minified code, which will look different.
